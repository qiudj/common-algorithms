package com.learning.algorithms.oj;

/**
 * OJ写法示例
 */
public class Main {
    public static void main(String[] args) {
/*        Scanner scanner = new Scanner(System.in);
        String[] sizeAndAimMoney = scanner.nextLine().split(" ");
        String[] moneyArr = scanner.nextLine().split(" ");

        int arrSize = Integer.parseInt(sizeAndAimMoney[0]);
        int aimMoney = Integer.valueOf(sizeAndAimMoney[1]);
        int[] arr = new int[moneyArr.length];
        for (int i = 0; i < arrSize; i++){
            arr[i] = Integer.valueOf(moneyArr[i]);
        }
        System.out.println(getMinCountOfMoney(arr, aimMoney));*/
    }
}